_Файл хранит и выполняет польностью бек для всех ИМЕННО кнопок, не полей, не тд  именно кнопок._
## Объявление переменных
**\_logInButton** -- переменная типа Button с модификатором \[SeializeField], назначается в инспекторе юнити и должна хранить кнопку входа на сцене log-in_screen

---
## public void Awake()
Добавляем "слушателя" для метода LogIn() при нажатии на кнопку в интерфейсе.

---
## public static void LogIn()
_хз нахуя статик перекомпилировать лень было вечером_
Вызываем функцию SwapSceneByName из класса SceneSwaper и передаем в нее главнй экранчик

### by @Rataty1ch1K